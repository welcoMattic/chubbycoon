framework:
    secret: '%env(APP_SECRET)%'
    default_locale: en
    csrf_protection: ~
    session:
        handler_id: ~
    php_errors:
        log: true
    translator:
        fallbacks: [ "en" ]
    assets:
        json_manifest_path: '%kernel.project_dir%/public/build/manifest.json'
